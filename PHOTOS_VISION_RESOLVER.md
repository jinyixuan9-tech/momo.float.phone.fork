# Photos v0.2.1 — Vision + Resolver

基于 `ii-phone custom baseline v0.2.0`。

## 本版新增

### 1. 自动识图
- 新上传照片自动进入识图队列。
- 每批最多 4 张图片，共用 1 次多模态请求；批次按顺序执行，避免同时轰炸 API。
- 已完成识图的照片不会因为重新打开 Photos 而再次调用 API。
- 图像识别使用当前已配置、且开启“图像识别”的聊天 API。
- 识图失败可在单张详情重试；图库页也提供“识别待处理”，只处理未完成/失败项。

### 2. 识图字段与人工修改
AI 只负责初始填写，人工修改优先。照片详情把主要信息收进一个“外观与场景”区：
- 内容描述 / 主体
- 发色（开放式文本，不限制固定色表）
- 配饰（眼镜、耳钉、项链、帽子等合并）
- 穿搭
- 季节倾向
- 场景
- 检索标签

人工改过的字段会记录为 manual，重新识别默认不覆盖。

### 3. 多人照片
- AI 只按 person_1 / person_2... 描述人物槽位，不猜真人身份。
- 每个人物槽位包含：位置、发色、配饰、穿搭、季节、场景。
- 用户手动把槽位映射到这张图已经关联的具体 char。

### 4. 角色当前特征
人物相册顶部新增“当前特征”（可选）：
- 可以直接手写，例如“浅金发、夏天、最近偶尔戴黑框眼镜”。
- 也可以从一张已识别的当前照片提取，再继续手动修改。
- 完全留空时，不做时期限制；该角色所有 AI 可调用照片都能正常参与候选。
- 设置后，Resolver 会在“现在/刚拍/今天”等意图中优先匹配当前特征；“以前/旧照/翻到之前”等意图则允许旧时期照片。

### 5. 三档图片来源策略
Photos → 图库顶部可分别设置聊天和朋友圈：
- 仅匹配相册
- 仅生图
- 优先相册，匹配不到再生图（默认）

“仅匹配相册”用于测试 Resolver：
- 聊天：找不到合适图时绝不偷跑生图，会让角色按人设自然化解（保密、下次给看、改天重拍等），不暴露系统/相册/匹配/AI。
- 朋友圈：找不到合适图时只发正文，不挂图，不生图。

### 6. Chat / Moments Resolver
角色原本准备生成图片时，现在可先经过 Photo Resolver：
- 关联角色必须匹配。
- 照片必须开启“AI 可调用”。
- 内容描述 / 主体 / 标签 / 外观 / 场景参与匹配。
- 当前特征（若设置）参与排序。
- 已发朋友圈的图默认不再作为“新图”主动私发 user。
- 私聊发过的照片仍可之后发朋友圈。
- 朋友圈不重复使用已经发过的同一张图。

### 7. 图片来源与真实感
- 聊天前端不显示“相册 / AI生成”等来源标签。
- 相册图沿用现有图片消息视觉样式。
- 只有点开图片详情时，才会低调显示“来自照片库”。
- 相册图不提供“重新生成”；AI 生成图保留原本的提示词/重新生成能力。

## v0.2.0 UI 修正
- “加入角色共享相册”和“AI 可调用”两张开关卡片统一高度、内边距和 toggle 对齐。

## 本版暂未做
- Bubble / SMS / X 接入。底层 Photo Resolver 已独立，可后续复用。
- embedding / 向量数据库。首版 Resolver 使用结构化识图信息和轻量文本相似度，便于观察匹配是否足够聪明。
- 可视化 Resolver 调试面板。当前可通过“仅匹配相册”模式验证是否真正命中图库。

## 建议测试
1. 确认 API 设置已开启“图像识别”。
2. 上传单人自拍、宠物/风景图、双人图各若干张。
3. 等识图完成，检查并手动修改识图字段。
4. 双人图把 person_1 / person_2 映射到具体 char。
5. 给角色设置当前特征，或选一张近期图作为参考；也测试完全不设置的情况。
6. 聊天策略设“仅匹配相册”，让角色发一个图库里明显存在的内容，确认直接发送真实照片。
7. 再请求一个图库里没有的内容，确认不生图、角色自然化解。
8. 切换“仅生图”，确认原生图链路不受影响。
9. 切换“优先相册，匹配不到再生图”，分别测试命中与未命中。
10. 朋友圈重复上述测试，并确认私聊用过的图仍能发朋友圈、朋友圈已经发过的图不会再主动私发为新图。
