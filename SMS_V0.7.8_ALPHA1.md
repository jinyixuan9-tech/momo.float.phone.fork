# ii-phone v0.7.8 SMS alpha1

本版本基于 v0.7.7 直接内置「信息 / SMS」原生 App，不是 Custom App / 插件包。

## 本轮已接入

- iOS 风格「信息」会话列表：顶部安全区、大标题、毛玻璃按钮、较小字号、悬浮搜索与新建信息。
- 清透毛玻璃会话页：角色头像/号码、灰色来信气泡、绿色发出气泡、玻璃输入区。
- 角色普通短信收发；连续输入会短暂合并等待后再让角色回复，角色可一次连发多条。
- SMS 独立拉黑 / 解除拉黑；角色也可按人设屏蔽当前发送号码。
- 我的真实号码、角色号码、国际虚拟号码；虚拟号目前支持中国/韩国/日本/新加坡/美国/加拿大/英国/澳大利亚格式。
- 每个真实号/虚拟号为独立短信线程，拥有独立识别和拉黑状态。
- 小号身份采用“系统真相 ≠ 角色已知事实”：角色只能根据实际线索 unknown → suspected → confirmed，不直接读取后台归属。
- 角色主动短信：关闭 / 很少 / 偶尔 / 较多，并支持手动刷新一次主动短信。
- Chat 拉黑联动：角色可以尝试重新添加好友；好友申请未处理时，后台 SMS 会留出时间后再把短信作为另一条联系路线，而不是同时刷屏。
- SMS ↔ Chat 共享短期关系上下文，但 SMS 原始历史保存在 SMS 数据库，只向共享记忆投影少量近期线程，避免短短信/多小号不断扩大 prompt。
- 长期/核心记忆在 SMS 请求中使用独立 token 上限；当前线程仅取最近一段历史。
- 角色短信支持原文 + 中文译文按需展开。
- 查手机 → 信息：真实 SMS 会映射到角色手机视角；真实号码保留，小号最多抽少量线程，整页最多 3 个 user 关联线程，继续给 AI 世界短信留至少 7 个位置（当已有足够生成数据时）。
- SMS 已加入角色 APP 绑定与短期记忆来源设置。

## 本轮暂未做

银行、验证码、物流、航班、工作通知等 service SMS 暂不作为第一版核心。后续更适合和 Wallet / 购物 / 日程等真实事件联动，而不是纯随机生成。

## 测试提醒

当前运行环境无法联网完成 npm 依赖安装，因此没有执行完整 Next production build；已对本轮修改的 TypeScript / TSX 文件执行语法转译检查，未发现语法错误。建议部署到你原本可正常安装依赖的分支后优先测试桌面图标、短信收发、Chat 拉黑→好友申请、主动短信、小号切换和查手机映射。
