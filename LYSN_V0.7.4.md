# LYSN v0.7.4

- 输入框内部的双向翻译箭头与目标字（简、繁、粤、英、日、韩、泰）；翻译预览在输入框下面。使用翻译后，发出的消息在紫色气泡中先显示译文，再用虚线隔开浅色原文。直接手打的消息保持单层。
- 每个聊天室分别设置艺人发消息、看粉丝消息、引用消息的偏好。自动看消息在手机打开期间由本地定时器处理，不调用生成 API，可逐条标记部分已读。艺人发布新消息时，之前的粉丝消息全部标记已读。已读为模拟状态，不代表真实的人查看。
- 粉丝消息左边有未读灰圈和已读紫色对勾。艺人消息翻译分隔线保留虚线。
- 语音详情的关闭键和头像正中央的播放键可点击；合成语音时跳过当前聊天室设置的粉丝称呼，文字记录仍完整。
- 略微下移聊天顶栏标题和 Bubble 标识，保留手机外壳顶部安全间距。

在项目目录安装依赖后运行 `npm run dev`。现有本地存储可继续使用，新字段由旧数据自动按默认值处理。
