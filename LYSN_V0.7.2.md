# LYSN v0.7.2

在 v0.7.1 基础上调整界面，不重置聊天数据。

- 艺人头像用细紫色渐变外圈，头像与外圈之间留透明间隔；缩小聊天头像及艺人标识。
- 顶栏订阅标志重新排版为 bubble 字样与渐变爱心，避免缩成难辨认的一条。
- OUR BOX 爱心、语言入口及紫底白箭头发送按钮统一尺寸。
- 语音气泡与译文分成上下两块；图片、语音总览排三列，语音卡片用深灰底。
- 语音详情使用整页深色背景、中央头像与叠在头像上的播放按钮。
- 长按打开消息菜单后，不再因随后产生的点击而切换译文。

用户消息翻译继续调用已有的翻译 URL/Key 配置。接口仍需与所填写服务的协议匹配。
